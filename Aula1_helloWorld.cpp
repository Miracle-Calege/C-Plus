#include <iostream> //->contem funcoes baiscas para operacoes de entrada e saida (biblioteca padrao)

int main() {//->metodo principal

    std::cout<<"Estou a escrever o meu primeiro codigo em c++"<<std::endl;//->std:: count <<  "texto"; e usado para imprimir e std::endl ou '\n' para mudar de linha;
    std::cout<<"Esta a ser muito bom";//todas as sentencas sao termidas por ";"


    return 0;//->o metodo main deve sempre retornar 0(isso guarante que o codigo e executado com sucesso)
}